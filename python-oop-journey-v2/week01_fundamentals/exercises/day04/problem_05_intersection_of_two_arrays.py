"""Problem 05: Intersection of Two Arrays

Topic: Hash Set, Intersection
Difficulty: Easy

Given two integer arrays, return their intersection.

Each element in the result must be unique. The result can be in any order.
"""

from __future__ import annotations


def intersection_of_two_arrays(nums1: list[int], nums2: list[int]) -> list[int]:
    """Find intersection of two arrays.

    Args:
        nums1: First list of integers
        nums2: Second list of integers

    Returns:
        List of unique elements present in both arrays

    Example:
        >>> sorted(intersection_of_two_arrays([1, 2, 2, 1], [2, 2]))
        [2]
        >>> sorted(intersection_of_two_arrays([4, 9, 5], [9, 4, 9, 8, 4]))
        [4, 9]
    """
    raise NotImplementedError("Implement intersection_of_two_arrays")
