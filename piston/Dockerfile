# Piston Code Execution Engine
# Minimal config - let base image handle everything

FROM ghcr.io/engineer-man/piston:latest

# Just expose the port
EXPOSE 2000

# Don't override ENTRYPOINT or CMD - base image handles it
